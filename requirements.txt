# DGX Spark Fine-Tuning Requirements
# ===================================
# Optimized for NVIDIA DGX Spark with Grace Blackwell architecture

# Core ML Framework
torch>=2.2.0
transformers>=5.5.3     # 5.5+ required for Gemma-4 (gemma4 config + Gemma4ForCausalLM)
datasets>=2.18.0
accelerate>=0.27.0

# PEFT (Parameter-Efficient Fine-Tuning)
peft>=0.10.0

# Quantization for QLoRA (training-time NF4) and NVFP4 export
bitsandbytes>=0.43.0
nvidia-modelopt[all]>=0.21.0   # NVFP4 post-training quantisation

# Tokenization
sentencepiece>=0.2.0
tiktoken>=0.6.0

# Training utilities
trl>=0.12.0             # SFTTrainer with packing support
wandb>=0.16.0           # Experiment tracking (optional)
tensorboard>=2.16.0     # Training visualization

# Flash Attention 2 (optional but recommended)
# Install separately: pip install flash-attn --no-build-isolation
# flash-attn>=2.5.0

# Evaluation
evaluate>=0.4.0
rouge-score>=0.1.2
nltk>=3.8.0

# Data processing
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=15.0.0         # Parquet support (fast columnar reads for 2.2M rows)

# Utilities
tqdm>=4.66.0
pyyaml>=6.0.0
rich>=13.0.0            # Pretty console output

# NVIDIA specific (pre-installed on DGX Spark)
# nvidia-ml-py>=12.0.0  # GPU monitoring
